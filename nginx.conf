events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Add support for JavaScript modules
    types {
        application/javascript mjs;
    }

    # Allow large file uploads (800MB)
    client_max_body_size 800M;

    upstream semorando {
        server semorando-api-prod:5000;
    }

    upstream semoutils {
        server semoutils-api-prod:8080;
    }

    # Redirect HTTP to HTTPS
    server {
        listen 80;
        server_name semotecapp localhost;
        return 301 https://$host$request_uri;
    }

    server {
        listen 443 ssl;
        http2 on;
        server_name semotecapp localhost;

        # SSL configuration
        ssl_certificate /etc/nginx/ssl/semotecapp+3.pem;
        ssl_certificate_key /etc/nginx/ssl/semotecapp+3-key.pem;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;
        ssl_prefer_server_ciphers on;

        # SemoRando .mjs files (JavaScript modules)
        location ~ ^/semorando/.*\.mjs$ {
            proxy_pass http://semorando-api-prod:5000;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_hide_header Content-Type;
            add_header Content-Type "application/javascript; charset=utf-8";
            add_header Cache-Control "public, max-age=31536000";
        }

        # SemoRando static files (assets, js, css, etc.)
        location ~ ^/semorando/.*\.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            proxy_pass http://semorando-api-prod:5000;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            add_header Cache-Control "public, max-age=31536000";
        }

        # SemoRando
        location /semorando/ {
            proxy_pass http://semorando-api-prod:5000/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Configuration pour SemOutils WebSocket (SignalR)
        location /semoutils/searchHub {
            proxy_pass http://semoutils-api-prod:8080/searchHub;
            proxy_http_version 1.1;

            # WebSocket support
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # Disable buffering for WebSocket
            proxy_buffering off;

            # Longer timeouts for SignalR
            proxy_connect_timeout 7d;
            proxy_send_timeout 7d;
            proxy_read_timeout 7d;
        }

        # SemOutils .mjs files (JavaScript modules)
        location ~ ^/semoutils/.*\.mjs$ {
            proxy_pass http://semoutils-api-prod:8080;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_hide_header Content-Type;
            add_header Content-Type "application/javascript; charset=utf-8";
            add_header Cache-Control "public, max-age=31536000";
        }

        # SemOutils static files (assets, js, css, etc.)
        location ~ ^/semoutils/.*\.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            proxy_pass http://semoutils-api-prod:8080;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            add_header Cache-Control "public, max-age=31536000";
        }

        # SemOutils
        location /semoutils/ {
            proxy_pass http://semoutils-api-prod:8080/;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }

        # Page par défaut
        location / {
            return 200 'Nginx Reverse Proxy - /semorando ou /semoutils';
            add_header Content-Type text/plain;
        }
    }
}
